"""Shared math utilities."""


def median(values):
    """Compute the median of a numeric list."""
    if not values:
        return None
    s = sorted(values)
    n = len(s)
    mid = n // 2
    return s[mid] if n % 2 else (s[mid - 1] + s[mid]) / 2
