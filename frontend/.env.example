# Vite Frontend Configuration

# Backend Flask server host (IP address or hostname)
# Default: 127.0.0.1 (localhost)
# For network access, use your machine's local IP (e.g., 192.168.1.100)
VITE_BACKEND_HOST=192.168.1.185

# Backend Flask server port
# Default: 5050 (must match config.json port)
VITE_BACKEND_PORT=5050

# Frontend dev server port
# Default: 3000
VITE_PORT=3000
