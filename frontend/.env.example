# Vite Frontend Configuration
#
# Backend host/port and frontend port are configured in ../config.json
# (single source of truth shared with the Flask backend).
#
# config.json fields used by Vite:
#   "host"           - Backend Flask server host (default: 127.0.0.1)
#   "port"           - Backend Flask server port (default: 5050)
#   "frontend_port"  - Frontend dev server port (default: 3000)
